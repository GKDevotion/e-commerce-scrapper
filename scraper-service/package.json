{
  "name": "amazon-listing-builder-scraper",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright-based Amazon product scraper sidecar for Amazon AI Listing Builder. Invoked as a child process from the Laravel app because PHP cannot run a headless browser natively.",
  "main": "scrape.js",
  "scripts": {
    "scrape": "node scrape.js",
    "postinstall": "playwright install --with-deps chromium"
  },
  "dependencies": {
    "playwright": "^1.45.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
